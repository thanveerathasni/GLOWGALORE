<!-- Page Header with Breadcrumbs -->


<!-- Sidebar / Dashboard Menu -->
<div class="col-md-4">
  <div class="dashboard-menu">
    <ul class="nav flex-column" role="tablist">
      <li class="nav-item">
        <a class="nav-link active" href="/dashboard" data-bs-toggle="tooltip" title="View your dashboard">
          <i class="fas fa-tachometer-alt mr-10"></i> Dashboard
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/address" data-bs-toggle="tooltip" title="Manage your addresses">
          <i class="fa-solid fa-location-dot"></i> My Address
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/wallet" data-bs-toggle="tooltip" title="Check your wallet">
          <i class="fas fa-wallet mr-10"></i> Wallet
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/orders" data-bs-toggle="tooltip" title="View your orders">
          <i class="fas fa-shopping-bag mr-10"></i> Orders
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/logout" data-bs-toggle="tooltip" title="Sign out">
          <i class="fas fa-sign-out-alt mr-10"></i> Logout
        </a>
      </li>
    </ul>
  </div>
</div>

<!-- Styling -->
<style>
  /* General Styling */
  body {
    background: linear-gradient(135deg, #f5d9ff 0%, #d8a9e3 100%);
    font-family: 'Poppins', sans-serif;
    overflow-x: hidden;
  }

  .container {
    max-width: 1300px;
  }

  /* Header */
  .page-header {
    background: linear-gradient(120deg, #f7e1fa 0%, #e3b0fa 100%);
    padding: 1.5rem 0;
    border-radius: 0 0 20px 20px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
  }

  .breadcrumb {
    font-size: 1.1rem;
    color: #6a4c93;
  }

  .breadcrumb a {
    color: #b22c99;
    text-decoration: none;
    position: relative;
    padding-bottom: 5px;
    transition: color 0.2s ease;
  }

  .breadcrumb a:hover {
    color: #f447ee;
  }

  .breadcrumb a::after {
    content: '';
    position: absolute;
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, #f07bdb, #ff99cc);
    bottom: 0;
    left: 0;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.3s ease;
  }

  .breadcrumb a:hover::after {
    transform: scaleX(1);
  }

  /* Sidebar / Dashboard Menu */
  .dashboard-menu {
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(10px);
    border-radius: 20px;
    padding: 1.5rem;
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
    position: sticky;
    top: 20px;
    transition: all 0.3s ease;
  }

  .dashboard-menu .nav-link {
    font-weight: 500;
    color: #8f498a;
    padding: 12px 20px;
    border-radius: 10px;
    margin-bottom: 10px;
    background: rgba(255, 255, 255, 0.2);
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .dashboard-menu .nav-link:hover {
    color: #ff66b3;
    background: rgba(255, 255, 255, 0.4);
    transform: translateX(8px) scale(1.05);
    box-shadow: 0 5px 15px rgba(255, 105, 180, 0.4);
  }

  .dashboard-menu .nav-link.active {
    background: linear-gradient(45deg, #ff66b3, #f07bdb);
    color: white;
    box-shadow: 0 5px 20px rgba(255, 105, 180, 0.6);
    border-left: 4px solid #ff66b3;
  }

  .dashboard-menu .nav-link::before {
    content: '';
    position: absolute;
    width: 4px;
    height: 100%;
    background: #ff66b3;
    left: 0;
    top: 0;
    transform: scaleY(0);
    transition: transform 0.3s ease;
  }

  .dashboard-menu .nav-link.active::before {
    transform: scaleY(1);
  }

  /* Cards */
  .card {
    border-radius: 25px;
    background: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(8px);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    margin-bottom: 20px;
    transition: all 0.3s ease;
  }

  .card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.15);
    transition: all 0.3s ease-in-out;
  }

  .card-header {
    background: linear-gradient(90deg, #d88ed6, #f1b7db);
    color: white;
    padding: 15px;
    border-radius: 25px 25px 0 0;
    font-weight: 600;
  }

  /* Buttons */
  .btn-primary {
    background: linear-gradient(45deg, #f07bdb, #ff99cc);
    border: none;
    padding: 12px 25px;
    border-radius: 12px;
    transition: all 0.3s ease;
  }

  .btn-primary:hover {
    background: linear-gradient(45deg, #e66fa3, #f3a8d6);
    transform: scale(1.05);
    box-shadow: 0 5px 15px rgba(255, 105, 180, 0.4);
  }

  .btn-success {
    background: linear-gradient(45deg, #a984b8, #d4aadf);
    border: none;
    padding: 10px 20px;
    border-radius: 10px;
    transition: all 0.3s ease;
  }

  .btn-success:hover {
    background: linear-gradient(45deg, #ab80b0, #d195c2);
    transform: scale(1.05);
  }

  /* Responsive Adjustments */
  @media (max-width: 768px) {
    .dashboard-menu {
      padding: 1rem;
      margin-bottom: 20px;
    }

    .dashboard-menu .nav-item {
      margin-bottom: 12px;
    }

    .card {
      border-radius: 15px;
    }

    .btn-primary, .btn-success {
      width: 100%;
      margin-bottom: 10px;
    }
  }

  @media (min-width: 992px) {
    .dashboard-menu {
      height: fit-content;
    }
  }

  /* Focus/Accessibility */
  .dashboard-menu .nav-link:focus {
    outline: 2px solid #f07bdb;
    background: rgba(255, 255, 255, 0.3);
  }
</style>
