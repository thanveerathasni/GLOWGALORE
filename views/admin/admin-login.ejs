<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login - Glow Galore</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        body {
            display: flex;
            height: 100vh;
            background-color: #faf5fe;
        }

        .container {
            display: flex;
            width: 100%;
            max-width: 1200px;
            margin: auto;
            background-color: white;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            height: 600px;
        }

        .form-section {
            padding: 40px;
            width: 50%;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .form-section h2 {
            font-size: 28px;
            margin-bottom: 20px;
            color: #2c3e50;
        }

        .form-section input {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
        }

        .form-section button {
            width: 100%;
            padding: 12px;
            background-color: #242628;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            margin: 10px 0;
        }

        .form-section button:hover {
            background-color: #353535;
        }

        .form-section p {
            text-align: center;
            margin-top: 10px;
            font-size: 14px;
        }

        .form-section a {
            color: #2c3e50;
            text-decoration: none;
        }

        

        @media (max-width: 768px) {
            .container {
                flex-direction: column;
            }

            .form-section, .image-section {
                width: 100%;
                height: 300px;
            } 
            .image-section {
            width: 50%;
            background-image: url('https://via.placeholder.com/600x800?text=Monstera+Plant');
            background-size: cover;
            background-position: center;
        }

            .image-section .img{
            height: 600px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="form-section">
            <h2>Admin Login</h2>
            <form id="admin-login-form" action="/admin/login" method="post">
                <input type="text" id="admin-username" name="email" placeholder="email" required>
                <input type="password" id="admin-password" name="password" placeholder="Password" required>
                <button type="submit">Log in</button>
            </form>
            <p><a href="/forgot-password">Forgot Password?</a></p>
        </div>
        <div class="image-section" style="height: 300px;width: 100px;">
            <img src="/images/icons/glow logo.png" style="height: 600px;">
        </div>

    </div>
</body>
</html>
